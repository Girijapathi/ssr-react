import React from 'react';

const Home = () => {
    return (
        <div>
            <div>I'm the home giii  component</div>
            <button onClick='()=>console.log("All is well now?!!")'>log a msg</button>
        </div>
    );
};

export default Home;